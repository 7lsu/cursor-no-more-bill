{
  "name": "cursor-no-more-bill",
  "version": "1.0.0",
  "description": "Cloudflare Worker 监控 Cursor 按量付费状态，自动关闭并通知",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240117.0",
    "typescript": "^5.3.3",
    "wrangler": "^3.24.0"
  }
}
